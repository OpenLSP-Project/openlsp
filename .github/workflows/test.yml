name: OpenLSP Tests

on:
  push:
  pull_request:

jobs:
  test:

    runs-on: ubuntu-latest

    steps:

    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.x"

    - name: Install test tools
      run: |
        python -m pip install --upgrade pip
        pip install pytest

    - name: Run tests
      run: |
        PYTHONPATH=openlsp-core pytest
